function addNumber(x: number, y: number): number {
    return x + y + 1;
}

let soma: number = addNumber(1, 2);
console.log(soma);


function Phone(call: number | string){
    return call;
}

async function getBD(id: number): Promise<string>{
    return ""
}